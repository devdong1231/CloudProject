spring:
  datasource:
    url: jdbc:h2:mem:cloudProject
    username: root
    password: 12345678
    driver-class-name: org.h2.Driver

  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true